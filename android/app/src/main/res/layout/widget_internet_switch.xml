<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widgetRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="horizontal"
    android:padding="4dp">

    <TextView
        android:id="@+id/widgetEnableButton"
        android:layout_width="58dp"
        android:layout_height="58dp"
        android:background="@drawable/widget_button_green_inactive"
        android:gravity="center"
        android:text="@string/widget_internet_on"
        android:textColor="#FFFFFF"
        android:textSize="14sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/widgetDisableButton"
        android:layout_width="58dp"
        android:layout_height="58dp"
        android:layout_marginStart="8dp"
        android:background="@drawable/widget_button_red_inactive"
        android:gravity="center"
        android:text="@string/widget_internet_off"
        android:textColor="#FFFFFF"
        android:textSize="14sp"
        android:textStyle="bold" />
</LinearLayout>
