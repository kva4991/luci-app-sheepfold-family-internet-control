# AI Context Sharing

> Current status: the router backend can append masked diagnostics and logs after explicit request flags, but the parent Android preview and opt-in controls are not implemented yet. The current UI sends only the parent's typed text (§aiimpl1).

Current Russian draft:

- [Передача контекста ИИ-помощнику](ai-context-sharing.ru.md)

Default behavior: send only the parent-typed message. Any Sheepfold context must be previewed and explicitly confirmed before being sent to an AI provider.
