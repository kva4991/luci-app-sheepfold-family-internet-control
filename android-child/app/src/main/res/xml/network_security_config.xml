<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      HTTPS используется первым. HTTP оставлен только как совместимый fallback
      для локальных адресов; код клиента отдельно запрещает HTTP вне LAN.
    -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
