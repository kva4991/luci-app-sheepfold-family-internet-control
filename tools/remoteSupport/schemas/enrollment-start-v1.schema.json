{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sheepfold.example/schemas/remote-support/enrollment-start-v1.schema.json",
  "title": "Sheepfold remote-support enrollment bootstrap v1",
  "description": "Unsigned request body carried only inside authenticated server TLS before a candidate router key is known.",
  "type": "object",
  "additionalProperties": false,
  "required": ["identityPublicKey", "clientNonce", "supportedVersions"],
  "properties": {
    "identityPublicKey": {
      "description": "Raw 32-byte Ed25519 public key as canonical base64url without padding.",
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]{43}$"
    },
    "clientNonce": {
      "description": "Fresh 32-byte nonce as canonical base64url without padding.",
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]{43}$"
    },
    "supportedVersions": {
      "type": "array",
      "minItems": 1,
      "maxItems": 4,
      "uniqueItems": true,
      "items": { "type": "integer", "minimum": 1, "maximum": 65535 }
    }
  }
}
